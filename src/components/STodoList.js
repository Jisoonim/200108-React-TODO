import React,{useEffect, useLayoutEffect} from "react"

const STodoList = () => {

    useLayoutEffect(()=> {
        console.log("TODO LIST.....")
    })

    return (
        <div>

        </div>
    )
}

export default STodoList